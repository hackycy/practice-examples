import 'package:flutter/widgets.dart';

void main() {
  runApp(
    const Center(
      child: Text(
        'Instead run:\nflutter run lib/xxx.dart',
        textDirection: TextDirection.ltr,
      ),
    ),
  );
}